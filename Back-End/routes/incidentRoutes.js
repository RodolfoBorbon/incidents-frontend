const express = require("express");
const router = express.Router();
const incidentController = require("../controllers/incidentController");

// Define routes for incident creation, update, management, etc.

module.exports = router;
