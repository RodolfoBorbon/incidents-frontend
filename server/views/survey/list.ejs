<%- include ../partials/header.ejs %>

<%- include ../partials/main_nav.ejs %>
  

  <!-- Main Content -->
  <main class="container">
    <div class="row">
        <div class="offset-md-1 col-md-11">
            <h1><%= title %></h1>

            <% if(typeof displayName == 'string' && displayName) { %>
                <a href="/survey-list/add" class="btn btn-primary" role="button">
                <i class="fas fa-plus-circle"></i> Add a Survey</a>
            <% } %>
            <br>
            <table class="table table-bordered table-striped table-hover">
                <!-- Header Row-->
                <tr class="d-flex">
                    <% if(typeof displayName == 'string' && displayName) { %>
                        <th class="text-center col-2">Title</th>
                        <th class="text-center col-2">Occupation</th>
                        <th class="text-center col-1">Attitude</th>
                        <th class="col-4">Gender</th>
                        <th class="text-center col-1">Feedback</th>
                        <th class="text-center col-1"></th>
                        <th class="text-center col-1"></th>
                    <% } else { %>
                        <th class="text-center col-2">Title</th>
                        <th class="text-center col-2">Occupation</th>
                        <th class="text-center col-1">Attitude</th>
                        <th class="col-6">Gender</th>
                        <th class="text-center col-1">Feedback</th>
                    <% } %>
                </tr>

                <!-- Repeatable Template Row -->

                <% for(let count = 0; count < SurveyList.length; count++) { %>
                    <tr class="d-flex">
                        <% if(typeof displayName == 'string' && displayName) { %>
                            <td class="text-center col-2"><%= SurveyList[count].name %></td>
                            <td class="text-center col-2"><%= SurveyList[count].occupation %></td>
                            <td class="text-center col-1"><%= SurveyList[count].attitude %></td>
                            <td class="col-4"><%= SurveyList[count].gender %></td>
                            <td class="text-center col-1"><%= SurveyList[count].feedback %></td>            
                            <td class="text-center col-1">
                                <a href="/Survey-list/edit/<%= SurveyList[count]._id %>" class="btn btn-primary btn-sm">
                                <i class="fas fa-pencil-alt"></i> Edit</a>
                            </td>
                            <td class="text-center col-1">
                                <a href="/Survey-list/delete/<%= SurveyList[count]._id %>" class="btn btn-danger btn-sm">
                                <i class="fas fa-trash-alt"></i> Delete</a>
                            </td>
                         <% } else { %>
                            <td class="text-center col-2"><%= SurveyList[count].name %></td>
                            <td class="text-center col-2"><%= SurveyList[count].occupation %></td>
                            <td class="text-center col-1"><%= SurveyList[count].attitude %></td>
                            <td class="col-6"><%= SurveyList[count].gender %></td>
                            <td class="text-center col-1"><%= SurveyList[count].feedback %></td>  
                         <% } %>
                    </tr>
                <% } %>
            </table>
        </div>
    </div>
  </main>
  
<%- include ../partials/bottom_nav.ejs %>  

<%- include ../partials/footer.ejs %>
  





